import React from 'react';
// import { Query } from 'react-apollo';
const App = () => {
  return (
    <div>
      {/*<Query query={GET_NOTES}>{() => null}</Query>*/}
      something
    </div>
  );
};

export default App;
